{
  "name": "agent-lens",
  "version": "2.0.0",
  "description": "Multi-agent usage dashboard for Claude Code, Codex, Gemini, OpenCode, Kimi, Cursor, Antigravity, and GitHub Copilot",
  "private": true,
  "bin": {
    "agent-lens": "server.js"
  },
  "scripts": {
    "start": "bun server.js",
    "dev": "bun --watch server.js",
    "test": "node --test",
    "benchmark": "node scripts/benchmark-api.js",
    "benchmark:cold": "node scripts/benchmark-cold-api.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.21.0"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
